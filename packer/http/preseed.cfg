# Locale and keyboard
d-i debian-installer/locale string en_US
d-i keyboard-configuration/xkb-keymap select us

# Network configuration
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string pdfchat-appliance
d-i netcfg/get_domain string local

# Mirror settings
d-i mirror/country string US
d-i mirror/http/hostname string archive.ubuntu.com
d-i mirror/http/directory string /ubuntu

# User account setup
d-i passwd/root-login boolean false
d-i passwd/user-fullname string PDF Chat User
d-i passwd/username string ubuntu
d-i passwd/user-password-crypted password $6$rounds=656000$salt$hashedpassword
d-i user-setup/allow-password-weak boolean true

# Partitioning
d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select atomic
d-i partman/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

# Package installation
d-i pkgsel/update-policy select none
d-i pkgsel/include string openssh-server python3 python3-pip nginx
d-i pkgsel/upgrade select full-upgrade

# Boot loader installation
d-i grub-installer/only_debian boolean true
d-i grub-installer/bootdev string default

# Finishing
d-i finish-install/reboot_in_progress note 