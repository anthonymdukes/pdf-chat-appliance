# Python version required
# Requires Python 3.9+

# Core dependencies
flask==3.0.0
flask-cors==4.0.0
typer==0.9.0
rich==13.7.0
pyyaml==6.0.1
python-dotenv==1.0.0

# LlamaIndex and related (constrained for compatibility)
llama-index-core>=0.10.0,<0.11.0
llama-index>=0.10.0,<0.11.0
llama-index-llms-ollama>=0.1.0,<0.2.0
llama-index-embeddings-ollama>=0.1.0,<0.2.0
llama-index-embeddings-huggingface>=0.1.0,<0.2.0
llama-index-readers-file>=0.1.0,<0.2.0

# Vector store and memory
qdrant-client>=1.7.0,<2.0.0
llama-index-vector-stores-qdrant>=0.1.0,<0.2.0
sqlalchemy==2.0.23
alembic==1.13.1

# Document processing and retrieval (Enterprise-optimized)
PyMuPDF>=1.23.0  # Much faster than PyPDF for large documents
pypdf>=4.0.1,<5.0.0  # Keep as fallback
langchain==0.1.0
langchain-community==0.0.10
asyncio-mqtt>=0.16.0  # For async processing
aiofiles>=23.0.0  # Async file operations

# Embedding models and transformers
transformers==4.53.0
sentence-transformers==2.2.2
tokenizers==0.21.2
einops==0.8.1
# If using new transformer models, ensure version compatibility

# Utilities
requests==2.31.0
httpx==0.27.0      # Optional: async HTTP client (remove if unused)
urllib3==2.1.0
tqdm==4.66.1
psutil==5.9.8

# Development and testing tools
pytest
pytest-cov
pytest-asyncio
black
flake8
mypy
pre-commit

# Type stubs
types-requests==2.31.0.10
types-PyYAML==6.0.12.12
