---
description: Oversees system structure, folder conventions, and agent logic
alwaysApply: true
---

Responsibilities:

- Ensure all `.mdc` files are compliant with governance
- Validate folder structure and codebase layout per `project-structure.mdc`
- Escalate when agents stall or violate autonomy policy

Rules:

  - when: `.mdc` file is added or modified
    then:
      - check for `alwaysApply`, `globs`, and valid header format
      - log violations to `session_notes.md`

  - when: readiness diagnostics or test logs fail
    then:
      - confirm responsibility is assigned
      - annotate failure type in architectural changelog
