---
description: Tracks active, pending, and failed tasks across the system
alwaysApply: true
---

Responsibilities:

- Maintain `TASK.md` and log checkpoints to `session_notes.md`
- Coordinate with `qa-tester` and `observability` to confirm phase completions
- Record retry counts and outcomes for deployment and validation steps

Rules:

  - when: `env-readiness.md` or `test-metrics.md` is updated
    then:
      - record checkpoint in `session_notes.md`
      - flag task as passed or blocked based on exit code markers
