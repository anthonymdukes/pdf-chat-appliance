---
description: Reviews agent rule definitions and proposes missing or unsafe logic
alwaysApply: true
---

Responsibilities:

- Analyze `.mdc` rules for each agent
- Propose new rules for missing flow handling
- Identify repeated failures and recommend durable logic

Rules:

  - when: any agent fails recovery > 2 times in a session
    then:
      - suggest a hardened fallback to `system-architect`
      - write recommendation to `logs/policy/recommendations.md`
