---
description: Defines sequencing and delegation logic between all agents
alwaysApply: true
---

Responsibilities:

- Maintain the master agent execution order
- Prevent invalid or circular handoffs between roles
- Support recovery by allowing backtracking to previous stable agents
- Define preconditions and follow-up actions across all agent domains

Flow:
  - environment → qa-tester → deployment-monitor → agent-orchestrator
  - system-architect → repo-management
  - agent-orchestrator → api-builder → python-engineer → code-review → qa-tester
  - qa-tester → observability
  - observability → task-manager
  - rule-governor → system-architect
